{
  "name": "kame-finance",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "setup:local": "bun run scripts/setup-local-dev.ts",
    "setup:supabase": "bun run scripts/setup-supabase.ts",
    "dev:local": "bun run scripts/setup-local-dev.ts --start",
    "dev:free-ports": "bash scripts/dev/free-local-ports.sh",
    "db:up": "docker compose up -d postgres",
    "db:down": "docker compose down",
    "dev": "turbo run dev",
    "ci:quality": "turbo run type-check lint test",
    "build": "turbo run build",
    "start": "turbo run start",
    "lint": "turbo run lint",
    "type-check": "turbo run type-check",
    "test": "turbo run test",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,md,json}\"",
    "db:generate": "turbo run db:generate",
    "db:migrate": "turbo run db:migrate",
    "db:push": "turbo run db:push",
    "db:seed": "turbo run db:seed --filter=@kame-finance/web",
    "db:studio": "turbo run db:studio",
    "clean": "turbo run clean && rm -rf node_modules"
  },
  "devDependencies": {
    "prettier": "^3.4.2",
    "turbo": "^2.4.0"
  },
  "packageManager": "bun@1.1.45",
  "engines": {
    "node": ">=20.0.0",
    "bun": ">=1.1.0"
  }
}
