/** Fixed Next.js port so this app can run beside kame-homes (:5173), kame-desk (:3100), and kame-lends (:3200). */
export const LOCAL_DEV_PORT = 3005;

export const LOCAL_DEV_APP_URL = `http://localhost:${LOCAL_DEV_PORT}`;
